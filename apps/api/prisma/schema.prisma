// Matches docs/data-model.md -- interaction-logging + voucher story.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql" // Neon free tier, both locally and hosted -- see docs/hosting.md
  url      = env("DATABASE_URL")
}

model Customer {
  id             String   @id @default(cuid())
  email          String   @unique
  name           String?
  activeTicketId String? // the Zendesk ticket currently receiving interaction comments
  createdAt      DateTime @default(now())

  carts             Cart[]
  orders            Order[]
  vouchers          Voucher[]
  interactionEvents InteractionEvent[]
  supportTickets    SupportTicket[]
}

model Product {
  id          String @id @default(cuid())
  name        String
  description String
  priceCents  Int
  imageUrl    String
  stock       Int

  cartItems  CartItem[]
  orderItems OrderItem[]
  vouchers   Voucher[]
}

model Cart {
  id                   String   @id @default(cuid())
  customerId           String?
  status               String   @default("active") // active | converted
  lastActivityAt       DateTime @default(now())
  utmSource            String?
  utmCampaign          String?
  utmContent           String?
  recommendationReason String?
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt

  customer Customer?  @relation(fields: [customerId], references: [id])
  items    CartItem[]
  order    Order?
}

model CartItem {
  id             String @id @default(cuid())
  cartId         String
  productId      String
  quantity       Int
  unitPriceCents Int

  cart    Cart    @relation(fields: [cartId], references: [id])
  product Product @relation(fields: [productId], references: [id])
}

model Order {
  id            String   @id @default(cuid())
  cartId        String   @unique
  customerId    String
  status        String   @default("paid")
  subtotalCents Int
  discountCents Int      @default(0)
  totalCents    Int
  voucherId     String?
  createdAt     DateTime @default(now())

  cart     Cart        @relation(fields: [cartId], references: [id])
  customer Customer    @relation(fields: [customerId], references: [id])
  voucher  Voucher?    @relation(fields: [voucherId], references: [id])
  items    OrderItem[]
}

model OrderItem {
  id             String @id @default(cuid())
  orderId        String
  productId      String
  quantity       Int
  unitPriceCents Int

  order   Order   @relation(fields: [orderId], references: [id])
  product Product @relation(fields: [productId], references: [id])
}

model Voucher {
  id              String    @id @default(cuid())
  code            String    @unique
  customerId      String
  productId       String
  percentOff      Int
  status          String    @default("active") // active | redeemed | expired
  expiresAt       DateTime
  issuedBy        String // agent
  resendCount     Int       @default(0)
  zendeskTicketId String?
  createdAt       DateTime  @default(now())

  customer Customer @relation(fields: [customerId], references: [id])
  product  Product  @relation(fields: [productId], references: [id])
  orders   Order[]
}

model InteractionEvent {
  id         String   @id @default(cuid())
  customerId String
  type       String // viewed_product | added_to_setup | removed_from_setup | answered_builder_step | tab_closed | returned_to_setup | activated | ...
  detail     String
  createdAt  DateTime @default(now())

  customer Customer @relation(fields: [customerId], references: [id])
}

model SupportTicket {
  id              String   @id @default(cuid())
  customerId      String
  zendeskTicketId String?
  subject         String
  message         String
  createdAt       DateTime @default(now())

  customer Customer @relation(fields: [customerId], references: [id])
}
