{
  "name": "oneix",
  "private": true,
  "description": "XLSmart connectivity storefront: email identification, live interaction logging, agent-issued vouchers, and a Zendesk ticket sidebar app. apps/web is one Next.js app (storefront + API) on MongoDB Atlas, deployed as a single Vercel project.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:zendesk-app": "npm run dev --workspace=apps/zendesk-app",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
